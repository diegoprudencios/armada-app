// ABOUTME: Barrel for the @armada/ui Button primitive (poc copy — overlaps crowdfund Button).
// ABOUTME: Copied components use `@/components/Button` (crowdfund API with icon prop).

export { Button } from './Button'
export type { ButtonProps, ButtonVariant, ButtonSize } from './Button'
